# Engine plan review — 2026-08-21

Reviewer: Stephen's Claude session (structured plan audit) plus an independent Codex (GPT-5.5) adversarial pass over the committed docs. Scope: `docs/superpowers/plans/2026-08-21-engine.md` + `PREP_PLAN.md` against the locked product contract.

## Verdict: SHIP WITH CHANGES — changes folded into this revision

The plan conforms to the contract's evidence hierarchy: detect-first with fault-agnostic Stage 1, machine explanation before any bearing claim, two-view corroboration, deterministic refusals that create tasks, human review before any work order, freeze-before-run evaluation with predeclared falsifiable expectations.

## Decisions (Stephen, 2026-08-21)

1. **Rule 01: cleared as starter scaffolding** per the event page wording ("starter scaffolds and existing libraries are fine"). The submission discloses the prep plainly. Vinh proceeds past Task 0.
2. **Repo split:** this repo stays private, always. Saturday's judged artifact lives in a fresh, clean public repo created at the event.
3. **All four v3 design changes approved:**
   - One-sided Stage-1 z rule (z >= +5, fault-physics direction). The build test falsified the two-sided rule: an injected pure tone suppresses crest/kurtosis and moves two groups. Verified fix, onset unchanged.
   - Harmonic floor (>= 3 harmonics above 3x floor for any element call) replacing the earlier FTF-guard proposal, which the v2 data showed would block the right call and miss the wrong one.
   - View A gate. This is the contract's own two-view rule; v1/v2 were envelope-only. Predeclared risk of converting correct calls to abstentions is accepted; results reported as they land.
   - Measured 1x shaft anchors Stage-2 labels only; every bearing-family prediction stays on the documented setpoint. Complies with the no-speed-inference cut; the contract discloses both values.
4. **Vocabulary sync:** task type renamed `INSPECT_BEARING` -> `INSPECTION_WORK_ORDER` throughout (matches the product contract's allowed task types; the MongoDB validators key on it).
5. **Kit:** pytest, pluggy, iniconfig, setuptools, and wheel added to the offline wheelhouse (73 wheels, manifest rehashed, quick verify PASS). pydantic + pydantic-core aarch64 were already staged.

## Notes for Saturday

- Evaluator numbers quoted anywhere judge-facing come from the frozen v3 run or a fresh Saturday freeze. v1/v2 are history, not claims.
- The slip finding (setpoint 35.0 Hz vs measured ~34.7 Hz, sidebands at 13.28 vs FTF 13.49) goes in the demo trust panel as live evidence for `TRUSTED_FOR_REPLAY` and the uncertainty windows.
- `JsonDecisionStore` is the dev store; the box uses a MongoDB store implementing the same `record()` protocol (Stephen's lane).
- B3_5 / B3_1 lead times are not quotable until the Task 14 onset inspection has a human verdict.

## Codex adversarial pass

One finding, verified against the source and folded into Task 14:

- **[MEDIUM, CONFIRMED] B3_5 "onset at window 9" is unreproducible under v3 semantics.** The v3
  `ReplayDetector` returns BASELINE for windows 1..10 and needs 3 consecutive ABNORMAL windows, so
  its earliest possible onset is window 11. The window-9 number came from the v2 prep evaluator,
  which must have used different (possibly baseline-inclusive) onset semantics. Consequence: v2 and
  v3 lead times are not comparable until the two onset definitions are reconciled, and neither
  B3_5's nor B3_1's lead time is quotable before then. Task 14 now starts with that reconciliation.

Codex also emitted generic un-pinned "next steps" assertions with no cited line or failing
scenario; per the team's verify-every-finding rule they were checked and not adopted.
